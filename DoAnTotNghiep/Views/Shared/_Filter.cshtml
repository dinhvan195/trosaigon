@using DoAnTotNghiep.Models
@{
    DBTro db = new DBTro();
}

<br />
<div class="container no-padding">
    @using (Html.BeginForm("Filter", "Post"))
    {
     <div class="row " style="background-color:black">
        <div class="form-group col-2">
            <label style="color:white">Loại tin</label>
                @Html.DropDownList("LoaiTin", null, "Tất cả", new { @class = "form-control" })
        </div>
        <div class="form-group col-2">
            <label style="color:white">Quận</label>
            @Html.DropDownList("Quan", null, "Tất cả", new { @class = "form-control" })
        </div>
        <div class="form-group col-2">
            <label style="color:white">Giá</label>
            @Html.DropDownList("Gia", null, "Tất cả", new { @class = "form-control" })
        </div>
        <div class="form-group col-2">
            <label style="color:white">Giới tính</label>
            @Html.DropDownList("DoiTuong", null, new { @class = "form-control" })
        </div>
        <div class="form-group col-4 text-right">
            <br />
            <input type="submit" value="Duyệt tin" class="btn btn-danger" style="width:60%"/>
        </div>
    </div>
    }
</div>
